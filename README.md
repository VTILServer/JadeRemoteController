## JadeRemoteController

# Notice
This is broken and most-likely needs to have major fixes before you can even **safely** deploy this into production.

Notable bugs include:
```txt
  1) Roblox Api Calls need to be updated to new Apis or else they'll fail.
  2) The Discord bot as a whole needs to be rewritten as it's currently in a unstable state.
  3) Secure Websocket connections failing opon login, I had to VC with TheJades to get this fixed in my version and even then it took him awhile to notice what the root cause was.
  4) The Code Itself.
```
